<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/kandinsky"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kandinsky"
        android:supportsRtl="true"
        android:theme="@style/Theme.InvestmentInConstruction"
        tools:targetApi="31">
        <activity
            android:name=".Authorization.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainBottomNavigation"/>
        <activity android:name=".Authorization.SignInActivity"/>
        <activity android:name=".Authorization.CreateAccountActivity"/>
        <activity android:name=".Room.EnterRoomActivity"/>
        <activity android:name=".Room.CreateRoomActivity"/>
        <activity android:name=".LoadingActivity"/>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>